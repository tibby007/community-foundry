import {describe,expect,it} from "vitest";
import {createProjectFromTemplate} from "@/data/templates";
import {campaignToMarkdown,toMarkdown,toJson,classroomToCsv,communityToCsv} from "@/domain/exports";
describe("launch package exports",()=>{const project=createProjectFromTemplate("consulting-client-accelerator","Women over 40");it("exports every required section without secrets",()=>{const md=toMarkdown(project);for(const heading of ["Foundation","Offer","Community","Classroom","Engagement","Promotion","Launch Score"])expect(md).toContain(`## ${heading}`);expect(md).toContain("30-Day Launch Plan");expect(md).not.toMatch(/OPENAI_API_KEY|SUPABASE_SERVICE_ROLE_KEY/i);expect(()=>JSON.parse(toJson(project))).not.toThrow()});it("creates quoted classroom and community CSV files",()=>{expect(classroomToCsv(project)).toContain('"Module"');expect(communityToCsv(project)).toContain('"Category"')});it("exports five posts, three emails, and thirty launch days",()=>{const campaign=campaignToMarkdown(project);expect(campaign.match(/^### Post/gm)).toHaveLength(5);expect(campaign.match(/^### Email/gm)).toHaveLength(3);expect(project.promotion.launchPlan).toHaveLength(30)})});
