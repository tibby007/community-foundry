import { ExternalLink,Signal } from "lucide-react";
import { getSignalsForTemplate } from "@/data/market-signals";
export function MarketValidation({templateId}:{templateId:string}){const signals=getSignalsForTemplate(templateId);return <section className="market-validation"><div className="market-title"><span><Signal size={14}/> MARKET SIGNAL VALIDATION</span><b>{signals.length} cited signals</b></div><div>{signals.map(signal=><article key={signal.id}><header><strong>{signal.label}</strong><span>{signal.strength} signal</span></header><p>{signal.summary}</p><a href={signal.sourceUrl} target="_blank" rel="noreferrer">{signal.sourceTitle} <ExternalLink size={11}/></a><small>Accessed {signal.accessedAt}</small></article>)}</div><footer>Curated summaries only. No Reddit posts, comments, or user data are stored.</footer></section>}
