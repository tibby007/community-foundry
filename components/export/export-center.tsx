"use client";
import {Download,FileJson,FileSpreadsheet,FileText,ImageIcon} from "lucide-react";
import type {CommunityProject} from "@/domain/project-schema";
import {campaignToMarkdown,classroomToCsv,communityToCsv,toJson,toMarkdown} from "@/domain/exports";
const slug=(value:string)=>value.toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-|-$/g,"");
function download(name:string,content:string,type:string){const url=URL.createObjectURL(new Blob([content],{type}));const anchor=document.createElement("a");anchor.href=url;anchor.download=name;anchor.click();URL.revokeObjectURL(url)}
export function ExportCenter({project}:{project:CommunityProject}){const name=slug(project.foundation.name);const files=[{label:"Launch blueprint",detail:"Markdown strategy and setup guide",icon:FileText,action:()=>download(`${name}-blueprint.md`,toMarkdown(project),"text/markdown")},{label:"Project data",detail:"Portable structured JSON",icon:FileJson,action:()=>download(`${name}.json`,toJson(project),"application/json")},{label:"Classroom",detail:"Modules and lessons CSV",icon:FileSpreadsheet,action:()=>download(`${name}-classroom.csv`,classroomToCsv(project),"text/csv")},{label:"Community setup",detail:"Categories and tags CSV",icon:FileSpreadsheet,action:()=>download(`${name}-community.csv`,communityToCsv(project),"text/csv")},{label:"Campaign pack",detail:"30-day plan, five posts, and three emails",icon:FileText,action:()=>download(`${name}-campaign.md`,campaignToMarkdown(project),"text/markdown")}];return <section className="export-center"><header><div><small>GUARANTEED DELIVERY</small><h2>Your launch package</h2></div><Download size={22}/></header><div>{files.map(file=><button key={file.label} onClick={file.action}><file.icon size={18}/><span><b>{file.label}</b><small>{file.detail}</small></span><Download size={14}/></button>)}</div><footer><ImageIcon size={14}/> Brand images download individually from Brand Studio. Export works without a Skool connection.</footer></section>}
