"use client";
import Image from "next/image";
import {useState} from "react";
import {Download,ImageIcon,RefreshCw} from "lucide-react";
import type {CommunityProject} from "@/domain/project-schema";
const assets=[{type:"icon",label:"Community icon",src:"/demo/community-icon.svg",width:180,height:180},{type:"cover",label:"Community cover",src:"/demo/community-cover.svg",width:480,height:240},{type:"promotion",label:"Launch graphic",src:"/demo/promo-graphic.svg",width:240,height:240}] as const;
export function BrandStudio({project}:{project:CommunityProject}){const [generated,setGenerated]=useState<Record<string,{url:string;provider:string}>>({});const [loading,setLoading]=useState<string|null>(null);async function generate(asset:typeof assets[number]){setLoading(asset.type);try{const response=await fetch("/api/images",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({assetType:asset.type,prompt:`${project.foundation.name}. ${project.foundation.promise}. ${project.brand.direction} visual direction using ${project.brand.palette.join(", ")}.`})});if(!response.ok)throw new Error("Image generation failed");const result=await response.json() as {url:string;provider:string};setGenerated(current=>({...current,[asset.type]:result}));}finally{setLoading(null)}}return <div className="brand-studio"><div className="brand-direction"><span><ImageIcon size={15}/> {project.brand.direction} direction</span><div>{project.brand.palette.map(color=><i key={color} style={{background:color}} title={color}/>)}</div><p>{project.brand.typography}</p></div><div className="brand-assets">{assets.map(asset=>{const result=generated[asset.type];const src=result?.url??asset.src;return <article key={asset.type}><div className={`asset-frame ${asset.type}`}><Image unoptimized={src.startsWith("data:")} src={src} alt={`${project.foundation.name} ${asset.label}`} width={asset.width} height={asset.height}/></div><footer><div><b>{asset.label}</b><small>{result?.provider==="openai"?"Generated with OpenAI":"Demo-safe generated fallback"}</small></div><a href={src} download><Download size={14}/><span className="sr-only">Download {asset.label}</span></a><button disabled={loading===asset.type} onClick={()=>generate(asset)} aria-label={`Generate variation for ${asset.label}`}><RefreshCw size={14}/></button></footer></article>})}</div></div>}
